// Esto constituye el 1° paso, o sea la creación del contexto Carrito

import { createContext } from "react";

export const CartContext = createContext(); // Invocamos a createContext() para darle contexto a CartContext